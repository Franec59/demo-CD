name: demo
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Hello world!!!"
    - uses: actions/checkout@main
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    - uses: azure/docker-login@v1
      with: 
        login-server: frl-kubernestes-dns-c30a276d.hcp.uksouth.azmk8s.io
        username: ${{ secrets.REGISTRY_USERNAME }}
        password: ${{ secrets.REGISTRY_PASSWORD }}
    -run: docker build . -t frl-kubernestes-dns-c30a276d.hcp.uksouth.azmk8s.io/demo
    -run: docker push frl-kubernestes-dns-c30a276d.hcp.uksouth.azmk8s.io/demo



    





